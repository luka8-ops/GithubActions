name: first-workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps: 
      - name: print greeting
        run: |
          echo "LUKIJANO MAKIJANO"
          echo "TEST"
      - name: print goodbye
        run: echo "POZDRAV" 
